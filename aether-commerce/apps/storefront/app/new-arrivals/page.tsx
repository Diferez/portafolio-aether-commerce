import { ProductGrid } from "../../components/ProductGrid";

export default function NewArrivalsPage() {
  return <ProductGrid initialFlag="new" heading="New arrivals" eyebrow="Fresh catalog" description="Recently normalized products and locally promoted arrivals." />;
}
