import { ProductGrid } from "../../components/ProductGrid";

export default function SearchPage() {
  return <ProductGrid heading="Search Aether" description="Search uses the Worker API and keeps Platzi data behind the Catalog Adapter." />;
}
